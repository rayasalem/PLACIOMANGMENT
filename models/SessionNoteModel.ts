
/**
 * RESPONSIBILITY: 
 * Manages qualitative data (notes/observations) attached to specific sessions.
 * These notes are for internal specialist/admin use and are never deleted.
 */
import { SessionNote } from '../types';

export type { SessionNote };
