
# Placio SaaS Deployment Guide 🚀

هذا الدليل يشرح خطوات نقل المنصة من بيئة التطوير إلى الإنتاج باستخدام أفضل الممارسات.

## 1. Frontend: Vercel
تعتبر Vercel الخيار الأفضل لاستضافة الواجهة الأمامية المبنية بـ React لسرعتها في توزيع المحتوى (CDN).

- **خطوات الربط**:
  1. اربط مستودع GitHub بـ Vercel.
  2. اختر الإعدادات الافتراضية لـ Vite/React.
  3. أضف المتغيرات البيئية (Environment Variables).

- **Environment Variables**:
  - `VITE_API_URL`: رابط الـ Backend على Railway (مثال: `https://placio-api.up.railway.app/api`).
  - `process.env.API_KEY`: مفتاح Gemini API الخاص بك.

---

## 2. Backend: Railway
تم اختيار Railway لسهولة ربطه بقواعد بيانات PostgreSQL ودعمه المتميز لـ Node.js.

- **خطوات النشر**:
  1. قم بإنشاء مشروع جديد على Railway.
  2. اربط مستودع الكود.
  3. أضف خدمة "PostgreSQL" من قائمة Add Service.
  
- **Environment Variables**:
  - `PORT`: 8080 (يتم تعيينه تلقائياً).
  - `DATABASE_URL`: يتم الحصول عليه تلقائياً عند ربط خدمة PostgreSQL.
  - `FRONTEND_URL`: رابط الـ Frontend على Vercel (للسماح بالـ CORS).
  - `JWT_SECRET`: سلسلة نصية طويلة وعشوائية لتأمين التوكنات.

---

## 3. Database: Managed PostgreSQL
- **الاستضافة**: يتم إدارتها بالكامل عبر Railway.
- **التجهيز**:
  1. افتح نافذة Query في لوحة تحكم PostgreSQL على Railway.
  2. انسخ محتويات ملف `database/schema.sql` ونفذها لإنشاء الجداول.

---

## 4. Build & Deploy Steps
1. **التحقق من الكود**: تأكد من أن جميع الـ imports تستخدم مسارات صحيحة.
2. **Push to GitHub**: بمجرد الدفع (Push) لفرع `main`:
   - ستقوم Railway ببناء الـ Docker Container الخاص بالـ Backend.
   - ستقوم Vercel ببناء الـ Static Assets الخاصة بالـ Frontend.
3. **Health Check**: تأكد من ظهور علامة "API Highspeed" في التطبيق.
